# homebrew-tap

Homebrew formulae for [enginerd-kr](https://github.com/enginerd-kr) tools.

```bash
brew install enginerd-kr/tap/grove
```

Formulae bump themselves: each tool's release workflow rewrites its formula's
version and checksums here — numbers only, over a deploy key scoped to this
repo — so a green release means an installable formula.
